# 🧪 แผนการทดสอบระบบ (Test Plan & QA Checklist)

เอกสารฉบับนี้รวบรวมฟีเจอร์หลักและขั้นตอนการทดสอบ (Test Cases) เพื่อใช้ตรวจสอบความถูกต้องของระบบหลังจากมีการเพิ่มฟีเจอร์ใหม่ๆ ในอนาคต

---

## 1. ระบบ Hero Banner (แบนเนอร์หลักหน้าที่ 1)
| หัวข้อทดสอบ | ขั้นตอนการทดสอบ | ผลลัพธ์ที่คาดหวัง |
| :--- | :--- | :--- |
| **Navigation Buttons** | เลื่อนเมาส์ไปกดที่แถบทรงสูงขอบซ้าย/ขวา | แบนเนอร์เปลี่ยนรูปตามทิศทางที่กด และมีการขยับของลูกศรเล็กน้อย |
| **Auto-Slide** | ปล่อยหน้าจอไว้เฉยๆ 5 วินาที | แบนเนอร์ต้องเปลี่ยนรูปภาพโดยอัตโนมัติ (Fade Transition) |
| **Indicators** | สังเกตจุดวงกลมด้านล่าง | จุดต้องเลื่อนตามลำดับภาพปัจจุบัน และกดเพื่อข้ามไปยังภาพนั้นๆ ได้ |
| **Responsive Height** | ย่อหน้าจอเป็นขนาดมือถือ | แบนเนอร์ต้องมีความสูงที่เหมาะสม (60vh) ไม่ดูแบนจนเกินไป |

## 2. ระบบสินค้าโปรโมชั่น (Bento Grid)
| หัวข้อทดสอบ | ขั้นตอนการทดสอบ | ผลลัพธ์ที่คาดหวัง |
| :--- | :--- | :--- |
| **Randomization** | กด Refresh หน้าเว็บ 2-3 ครั้ง | สินค้าในกล่อง Bento ต้องเปลี่ยนรายการหรือสลับตำแหน่งแบบสุ่ม |
| **Discount Badge** | ตรวจสอบป้ายสีแดงมุมขวาบน | ต้องแสดงเปอร์เซ็นต์ส่วนลดที่ถูกต้อง (คำนวณจาก Original vs Current Price) |
| **Price Logic** | ตรวจสอบราคาใน Hero Card (ใบใหญ่) | ต้องแสดงราคาเต็มขีดฆ่า และราคาลดปัจจุบัน พร้อมป้าย Limited Offer |
| **Grid Integrity** | ตรวจสอบจำนวนสินค้า | ต้องแสดงสินค้าครบ 7 ชิ้นในรูปแบบ Bento (1 ใหญ่, 2 กลาง, 4 เล็ก) |

## 3. ระบบบทความ (Blog Highlights)
| หัวข้อทดสอบ | ขั้นตอนการทดสอบ | ผลลัพธ์ที่คาดหวัง |
| :--- | :--- | :--- |
| **Random Articles** | ดูส่วน "บทความที่น่าสนใจ" ท้ายหน้า | ต้องสุ่มบทความขึ้นมา 3 เรื่อง ไม่ซ้ำกัน |
| **Premium UI** | สังเกตดีไซน์การ์ดบทความ | มี Image Overlay แยกระหว่างภาพและเนื้อหา และมีชื่อผู้เขียน/วันที่ชัดเจน |
| **Links** | กดที่ชื่อบทความ หรือปุ่ม "อ่านบทความทั้งหมด" | ต้องลิงก์ไปยังหน้า `/articles` ได้ถูกต้อง |

## 4. ระบบการกรองสินค้า (Filter & Search)
| หัวข้อทดสอบ | ขั้นตอนการทดสอบ | ผลลัพธ์ที่คาดหวัง |
| :--- | :--- | :--- |
| **Category Filter** | กดเลือกหมวดหมู่ที่ Sidebar | รายการสินค้าด้านขวาต้องเปลี่ยนตามหมวดที่เลือก และซ่อน Bento Grid |
| **Search Function** | พิมพ์ชื่อสินค้าในช่องค้นหา | ระบบต้องแสดงสินค้าที่ตรงกับคำค้นหาทันที (Real-time filtering) |
| **Empty State** | ค้นหาคำที่ไม่มีในระบบ | ต้องแสดงข้อความ "ไม่พบสินค้า" ในดีไซน์ที่สวยงาม |

## 5. ระบบหน้าโปรโมชั่นพิเศษ (Deals Page)
| หัวข้อทดสอบ | ขั้นตอนการทดสอบ | ผลลัพธ์ที่คาดหวัง |
| :--- | :--- | :--- |
| **Exclusive Sale** | เข้าไปที่หน้า `/deals` | ต้องแสดงเฉพาะสินค้าที่มีการลดราคา (`originalPrice > price`) เท่านั้น |
| **Landing UI** | ตรวจสอบ Header ของหน้า | มีการใช้โทนสีแดง (Hot Deals) เพื่อเน้นย้ำว่าเป็นหน้าลดราคา |

---

## 🛠 คำแนะนำสำหรับการพัฒนาต่อ (Future Development)
หากต้องการทำระบบ Test ให้เป็นอัตโนมัติ (Automated Testing) แนะนำให้ติดตั้ง **Playwright** หรือ **Cypress** เพิ่มเติม โดยใช้คำสั่ง:
```bash
npm init playwright@latest
```
จะทำให้เราสามารถเขียนสคริปต์ให้บอทวิ่งเทสทุกหน้าให้อัตโนมัติก่อนที่จะ Deploy งานครับ
